export function reducer(state, { type, payload }) {
    switch (type) {
        // case 'SET_GOODS':
        //     return {
        //         ...state,
        //         goods: payload.items || [],
        //         loading: false
        //     }
        default:
            return state;
    }

}